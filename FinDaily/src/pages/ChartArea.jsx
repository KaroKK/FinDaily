import React, { useEffect, useState } from "react";
import "./ChartArea.css";

function ChartArea({ token, filterParams }) {
  const [chartData, setChartData] = useState(null);
}

export default ChartArea;
